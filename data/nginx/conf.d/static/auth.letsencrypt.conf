# acme auth path for webroot, using letsencrypt certbot
# TODO: for server in China mainland, try not use letsencrypt.
location /.well-known/acme-challenge/ {
        default_type "text/plain";
        alias /webroot/;
}
